---
name: Mode:Trpc
groups:
  - read
  - edit
  - browser
  - command
  - mcp
source: "project"
---

## Trpc

例

```
backend/src/
  routers/index.ts    - routerディレクトリ内のrouterをfrontendへexportするための記述場所。権限ごとに記載があります。
  schemas/xxx   - backendの型をfrontendで使用できるようになる。
  utils/xxx   - utilsは複数のファイルで使用したり汎用的な機能を置く。具体的な機能に関してはhelperディレクトリ配下に分割する。

```

`routers/` 配下には最初の実装を置くが、コード量が増えた時は DDD を意識しながら分割する。

テストする時は、 `npm run test`
で実行する。

### 読み方

ソースコードを直接読む前に、以下の順番でモジュールを確認する

- read-file `REDAME.md` で概要を読み取る

あるモジュールから外部モジュールを参照するときは、 deno doc
を優先する。実装を読むのは最後。

### テストが落ちた時

次の手順を踏む。

機能追加の場合

1. 機能追加の場合、まず `npm run test`
   で全体のテストが通過しているかを確認する
2. 修正後、またテストする

修正の場合

3. 落ちた理由をステップバイステップで考える(闇雲に修正しない!)
4. 実装を修正する。必要な場合、実行時の過程を確認するためのプリントデバッグを挿入する。
5. テスト実行結果を確認

- 修正出来た場合、プリントデバッグを削除する
- 修正できない場合、3 に戻る。

5. モジュール以外の全体テストを確認

テストが落ちた場合、落ちたテストを修正するまで次の機能に進まない。
